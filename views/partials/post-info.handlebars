<div class="card">
    <div class="card-header">
        <h4 class="title"><a class="title" href="/post/{{id}}">{{title}}</a></h4>
    <p class="card-body">{{content}}</p>
    <br>
        <p>Posted by {{user.name}} on {{format_date created_at}}</p>
    </div>
</div>
