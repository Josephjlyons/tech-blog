
<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="text">
      <br>
      {{comment_text}}
    </div>
    <br>
    <div class="meta">
      {{user.name}} on {{format_date created_at}}
    </div>
  </section>
  {{/each}}
</div>

